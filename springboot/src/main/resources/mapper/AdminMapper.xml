<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AdminMapper">

    <select id="selectAll" resultType="com.example.entity.VO.AdminVO">
        select * from `admin`
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userInfo != null">
                and ( username like concat(#{userInfo}, '%') OR name like concat('%', #{userInfo}, '%'))
            </if>
        </where>
    </select>

<!--    <insert id="insert" parameterType="com.example.entity.Admin" useGeneratedKeys="true">-->
<!--        insert into `admin` (id, username, password, name, phone, email, avatar, role)-->
<!--        values (#{id}, #{username}, #{password}, #{name}, #{phone}, #{email}, #{avatar}, #{role})-->
<!--    </insert>-->
    <insert id="insert" parameterType="com.example.entity.Admin" useGeneratedKeys="true">
        insert into `admin` (id, username, password, salt, name, phone, email, avatar)
        values (#{id}, #{username}, #{password}, #{salt}, #{name}, #{phone}, #{email}, #{avatar})
    </insert>

    <update id="updateById" parameterType="com.example.entity.Admin">
        update `admin`
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="salt != null">
                salt = #{salt},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
<!--            <if test="role != null">
                role = #{role},
            </if>-->
        </set>
        where id = #{id}
    </update>

    <delete id="deleteBatch" parameterType="java.util.List">
        delete from `admin` where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!--验证判断-->
    <select id="existsByUserName" resultType="java.lang.Boolean">
        SELECT EXISTS(select *
                      from `admin`
                      where username = #{username})
    </select>

</mapper>