<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.NotificationMapper">

    <insert id="insert" parameterType="com.example.entity.Notification" useGeneratedKeys="true" keyProperty="id">
        insert into notification (user_id, type, content, is_read, create_time)
        values (#{userId}, #{type}, #{content}, #{isRead}, #{createTime})
    </insert>

    <select id="selectByUserId" resultType="com.example.entity.Notification">
        select * from notification
        where user_id = #{userId}
        order by create_time desc
    </select>

    <update id="markAsRead" parameterType="java.lang.Integer">
        update notification
        set is_read = 1
        where id = #{id}
    </update>

    <update id="markAllAsRead" parameterType="java.lang.Integer">
        update notification
        set is_read = 1
        where user_id = #{userId}
    </update>

    <select id="countUnreadByUserId" resultType="java.lang.Integer">
        select count(*) from notification
        where user_id = #{userId}
          and is_read = 0
    </select>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from notification where id = #{id}
    </delete>

    <delete id="deleteByUserId" parameterType="java.lang.Integer">
        delete from notification where user_id = #{userId}
    </delete>

    <select id="getAll" resultType="com.example.entity.Notification">
        select * from notification
        order by create_time desc
    </select>

</mapper>